{
  "version": 2,
  "buildCommand": "pnpm install && pnpm build && rm -rf .vercel/output && mkdir -p .vercel/output/static && cp -r frontend/dist/* .vercel/output/static/",
  "outputDirectory": ".vercel/output/static"
}

